export const metadata = {
  title: "Manifesto | Síntesi",
  description: "Our story and vision for better business organization",
}

export default function ManifestoLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}

